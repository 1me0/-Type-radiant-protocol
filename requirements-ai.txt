# ============================================================
# Radiant Protocol – AI Service Dependencies
# ============================================================
# This file lists the Python packages required for the
# Radiant Protocol AI scoring engine, truth verification,
# and falsification backend.
#
# Install with: pip install -r requirements-ai.txt
# ============================================================

# Core ML / Deep Learning
torch==2.8.0                     # PyTorch (CUDA 11.8 / CPU fallback)
transformers==5.0.0rc3             # Hugging Face Transformers (LLMs, NLI)
sentence-transformers==2.2.2     # Embeddings (all-MiniLM-L6-v2)
datasets==2.15.0                 # Dataset loading (for MNLI training)

# Web Framework & API
fastapi==0.104.1                 # High‑performance async API
uvicorn[standard]==0.24.0        # ASGI server (with standard extras)
pydantic==2.4.2                  # Data validation (v2)

# Utilities
requests==2.33.0                 # HTTP client (for external APIs)
numpy==1.24.3                    # Numerical computing
python-dotenv==1.0.0             # Environment variables (for API keys)

# Optional: add for better performance
# - torchvision, torchaudio (if needed)
# - scikit-learn (for clustering)
# - faiss-cpu (for vector search)

# ============================================================
# Pin all versions for reproducibility.
# Upgrade with caution and test thoroughly.
# ============================================================
